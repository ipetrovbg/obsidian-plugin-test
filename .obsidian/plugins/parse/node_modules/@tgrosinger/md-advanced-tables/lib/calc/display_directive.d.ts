import { IToken } from 'ebnf';
export interface Formatter {
    format(num: number | string): string;
}
export declare class DefaultFormatter {
    format: (num: number | string) => string;
}
export declare class DisplayDirective {
    private readonly decimalLength;
    constructor(ast: IToken);
    format: (num: number | string) => string;
}
