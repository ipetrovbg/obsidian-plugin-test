import { Result } from '../neverthrow/neverthrow';
import { Table } from '../table';
import { Cell, ValueProvider } from './ast_utils';
import { Value } from './results';
import { IToken } from 'ebnf';
export declare class Range implements ValueProvider {
    private readonly startRow;
    private readonly startColumn;
    private readonly endRow;
    private readonly endColumn;
    constructor(ast: IToken, table: Table);
    readonly getValue: (table: Table, currentCell: Cell) => Result<Value, Error>;
    readonly asCells: () => Result<Cell[], Error>;
}
