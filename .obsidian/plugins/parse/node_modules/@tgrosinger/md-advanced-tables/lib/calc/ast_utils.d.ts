import { Result } from '../neverthrow/neverthrow';
import { Table } from '../table';
import { Value } from './results';
import { IToken } from 'ebnf';
export declare const errIndex0: Error;
export declare const errRelativeReferenceIndex: Error;
export declare const checkType: (ast: IToken, ...expectedTypes: string[]) => Error | undefined;
export declare const checkChildLength: (ast: IToken, len: number) => Error | undefined;
export interface ValueProvider {
    getValue(table: Table, currentCell: Cell): Result<Value, Error>;
}
export interface Cell {
    row: number;
    column: number;
}
export declare const prettyPrintAST: (token: IToken, level?: number) => void;
